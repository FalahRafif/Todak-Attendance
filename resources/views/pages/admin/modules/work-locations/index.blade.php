@extends('layouts.admin.admin')
@section('title', $title)
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Kelola lokasi kerja, radius, dan koordinat GPS.</p></div><a href="{{ route('admin.work-locations.create') }}" class="btn btn-primary shadow-sm">Create Location</a></div>
<div class="card custom-card ka-card"><div class="ka-card-header"><div><h5 class="mb-1">Work Location List</h5><span class="text-muted">{{ $items->total() }} total data</span></div><input type="search" class="form-control ka-search" placeholder="Search location..." data-table-search="ka-location-table"></div><div class="card-body p-0"><div class="table-responsive"><table class="table ka-table" id="ka-location-table"><thead><tr><th>Name</th><th>Address</th><th>Coordinate</th><th>Radius</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@foreach($items as $item)<tr><td><div class="d-flex align-items-center gap-3"><span class="ka-avatar">{{ strtoupper(substr($item->name, 0, 1)) }}</span><span class="fw-semibold">{{ $item->name }}</span></div></td><td>{{ $item->address ?? '-' }}</td><td><span class="text-muted small">{{ $item->latitude ?? '-' }}, {{ $item->longitude ?? '-' }}</span></td><td><span class="ka-badge ka-badge-primary">{{ $item->radius_meter }} m</span></td><td><span class="ka-badge {{ $item->is_active ? 'ka-badge-success' : 'ka-badge-muted' }}">{{ $item->is_active ? 'Active' : 'Inactive' }}</span> @if($item->is_default)<span class="ka-badge ka-badge-primary">Default</span>@endif</td><td class="text-end"><div class="ka-action-group"><a href="{{ route('admin.work-locations.edit', $item->id) }}" class="btn btn-sm btn-primary">Edit</a><form method="POST" action="{{ route('admin.work-locations.destroy', $item->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></div></td></tr>@endforeach</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div>
@endsection
@push('scripts')@include('pages.admin.modules.partials.table-search')@endpush
