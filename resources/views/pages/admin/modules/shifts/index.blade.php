@extends('layouts.admin.admin')
@section('title', $title)
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Kelola jam kerja dan toleransi keterlambatan.</p></div><a href="{{ route('admin.shifts.create') }}" class="btn btn-primary shadow-sm">Create Shift</a></div>
<div class="card custom-card ka-card"><div class="ka-card-header"><div><h5 class="mb-1">Shift List</h5><span class="text-muted">{{ $items->total() }} total data</span></div><input type="search" class="form-control ka-search" placeholder="Search shift..." data-table-search="ka-shift-table"></div><div class="card-body p-0"><div class="table-responsive"><table class="table ka-table" id="ka-shift-table"><thead><tr><th>Name</th><th>Time</th><th>Tolerance</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@foreach($items as $item)<tr><td><div class="d-flex align-items-center gap-3"><span class="ka-avatar">{{ strtoupper(substr($item->name, 0, 1)) }}</span><span class="fw-semibold">{{ $item->name }}</span></div></td><td>{{ substr($item->start_time, 0, 5) }} - {{ substr($item->end_time, 0, 5) }}</td><td><span class="ka-badge ka-badge-primary">{{ $item->late_tolerance_minutes }} min</span></td><td><span class="ka-badge {{ $item->is_active ? 'ka-badge-success' : 'ka-badge-muted' }}">{{ $item->is_active ? 'Active' : 'Inactive' }}</span> @if($item->is_overnight)<span class="ka-badge ka-badge-primary">Overnight</span>@endif</td><td class="text-end"><div class="ka-action-group"><a href="{{ route('admin.shifts.edit', $item->id) }}" class="btn btn-sm btn-primary">Edit</a><form method="POST" action="{{ route('admin.shifts.destroy', $item->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></div></td></tr>@endforeach</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div>
@endsection
@push('scripts')@include('pages.admin.modules.partials.table-search')@endpush
