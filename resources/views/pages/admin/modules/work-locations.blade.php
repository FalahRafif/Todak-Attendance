@extends('layouts.admin.admin')
@section('title', $title)
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="page-header-breadcrumb mb-3"><h2 class="main-content-title fs-24 mb-1">{{ $title }}</h2></div>
<div class="card custom-card"><div class="card-body"><form method="POST" action="{{ route('admin.work-locations.store') }}" class="row g-3">@csrf <div class="col-md-3"><input name="name" class="form-control" placeholder="Name" required></div><div class="col-md-3"><input name="address" class="form-control" placeholder="Address"></div><div class="col-md-2"><input name="latitude" class="form-control" placeholder="Latitude"></div><div class="col-md-2"><input name="longitude" class="form-control" placeholder="Longitude"></div><div class="col-md-1"><input name="radius_meter" type="number" value="100" class="form-control" required></div><div class="col-md-1"><button class="btn btn-primary w-100">Save</button></div><div class="col-md-2"><label><input type="checkbox" name="is_default" value="1"> Default</label></div><div class="col-md-2"><label><input type="checkbox" name="is_active" value="1" checked> Active</label></div></form></div></div>
<div class="card custom-card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Name</th><th>Address</th><th>GPS</th><th>Radius</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($items as $item)<tr><form method="POST" action="{{ route('admin.work-locations.update', $item->id) }}">@csrf @method('PUT')<td><input name="name" value="{{ $item->name }}" class="form-control" required></td><td><input name="address" value="{{ $item->address }}" class="form-control"></td><td><input name="latitude" value="{{ $item->latitude }}" class="form-control mb-1"><input name="longitude" value="{{ $item->longitude }}" class="form-control"></td><td><input name="radius_meter" type="number" value="{{ $item->radius_meter }}" class="form-control" required></td><td><label><input type="checkbox" name="is_default" value="1" @checked($item->is_default)> Default</label><br><label><input type="checkbox" name="is_active" value="1" @checked($item->is_active)> Active</label></td><td><button class="btn btn-sm btn-primary">Update</button></form><form method="POST" action="{{ route('admin.work-locations.destroy', $item->id) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@endforeach</tbody></table>{{ $items->links() }}</div></div>
@endsection
