@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Cek koreksi absensi dan beri keputusan.</p></div><a href="{{ route('hrd.attendance-corrections') }}" class="btn btn-light">Kembali</a></div><div class="row g-3"><div class="col-lg-8"><div class="card custom-card ka-card"><div class="card-body"><h5>{{ $item->employee?->full_name ?? '-' }}</h5><p class="text-muted">{{ $item->employee?->department?->name ?? '-' }}</p><div class="row g-3"><div class="col-6 col-md-4"><div class="ka-detail-box"><b>Tanggal</b><div>{{ $item->correction_date?->format('d M Y') }}</div></div></div><div class="col-6 col-md-4"><div class="ka-detail-box"><b>Masuk Diajukan</b><div>{{ $item->requested_check_in_at?->format('Y-m-d H:i') ?? '-' }}</div></div></div><div class="col-6 col-md-4"><div class="ka-detail-box"><b>Pulang Diajukan</b><div>{{ $item->requested_check_out_at?->format('Y-m-d H:i') ?? '-' }}</div></div></div><div class="col-12"><div class="ka-detail-box"><b>Alasan</b><div>{{ $item->reason ?? '-' }}</div></div></div><div class="col-12"><div class="ka-detail-box"><b>Alasan Ditolak</b><div>{{ $item->rejected_reason ?? '-' }}</div></div></div></div></div></div></div><div class="col-lg-4"><div class="card custom-card ka-card"><div class="card-body"><h5>Keputusan</h5><p>Status: <b>{{ friendly_label($item->status?->description) }}</b></p><form method="POST" action="{{ route('hrd.attendance-corrections.approve', $item->id) }}" class="mb-2">@csrf<textarea name="approval_note" class="form-control mb-2" placeholder="Catatan persetujuan"></textarea><button class="btn btn-success w-100">Setujui</button></form><form method="POST" action="{{ route('hrd.attendance-corrections.reject', $item->id) }}">@csrf<textarea name="rejected_reason" class="form-control mb-2" placeholder="Alasan ditolak" required></textarea><button class="btn btn-danger w-100">Tolak</button></form></div></div></div></div></div>
@endsection
