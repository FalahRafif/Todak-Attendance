@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Tinjau koreksi jam absensi dari karyawan.</p></div></div><div class="card custom-card ka-card"><div class="card-body"><div class="ka-hrd-card-list">@forelse($items as $item)<a href="{{ route('hrd.attendance-corrections.show', $item->id) }}" class="ka-hrd-item"><div class="d-flex justify-content-between gap-2"><div><div class="ka-hrd-item-title">{{ $item->employee?->full_name ?? '-' }}</div><div class="ka-hrd-meta">{{ $item->correction_date?->format('d M Y') }}</div></div><span class="ka-hrd-pill">{{ friendly_label($item->status?->description) }}</span></div><div class="ka-hrd-meta mt-2">Masuk {{ $item->requested_check_in_at?->format('H:i') ?? '-' }} · Pulang {{ $item->requested_check_out_at?->format('H:i') ?? '-' }}</div></a>@empty<div class="text-center text-muted py-4">Belum ada koreksi.</div>@endforelse</div><div class="table-responsive ka-hrd-table"><table class="table ka-table"><thead><tr><th>Karyawan</th><th>Tanggal</th><th>Masuk Diajukan</th><th>Pulang Diajukan</th><th>Status</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->employee?->full_name ?? '-' }}</td><td>{{ $item->correction_date?->format('Y-m-d') }}</td><td>{{ $item->requested_check_in_at?->format('H:i') ?? '-' }}</td><td>{{ $item->requested_check_out_at?->format('H:i') ?? '-' }}</td><td>{{ friendly_label($item->status?->description) }}</td><td><a href="{{ route('hrd.attendance-corrections.show', $item->id) }}" class="btn btn-sm btn-primary">Detail</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Belum ada koreksi.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div></div>
@endsection
