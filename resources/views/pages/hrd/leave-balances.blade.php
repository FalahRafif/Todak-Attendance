@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Pantau saldo cuti karyawan.</p></div></div><div class="card custom-card ka-card"><div class="card-body"><div class="ka-hrd-card-list">@forelse($items as $item)<div class="ka-hrd-item"><div class="d-flex justify-content-between gap-2"><div><div class="ka-hrd-item-title">{{ $item->employee?->full_name ?? '-' }}</div><div class="ka-hrd-meta">{{ $item->employee?->department?->name ?? '-' }} · {{ friendly_label($item->leaveType?->description) }}</div></div><span class="ka-hrd-pill">{{ $item->year }}</span></div><div class="ka-hrd-meta mt-2">Kuota {{ $item->total_quota }} · Terpakai {{ $item->used_quota }} · Sisa {{ $item->remaining_quota }}</div></div>@empty<div class="text-center text-muted py-4">Belum ada saldo cuti.</div>@endforelse</div><div class="table-responsive ka-hrd-table"><table class="table ka-table"><thead><tr><th>Karyawan</th><th>Departemen</th><th>Jenis</th><th>Tahun</th><th>Kuota</th><th>Terpakai</th><th>Sisa</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->employee?->full_name ?? '-' }}</td><td>{{ $item->employee?->department?->name ?? '-' }}</td><td>{{ friendly_label($item->leaveType?->description) }}</td><td>{{ $item->year }}</td><td>{{ $item->total_quota }}</td><td>{{ $item->used_quota }}</td><td>{{ $item->remaining_quota }}</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">Belum ada saldo cuti.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div></div>
@endsection
