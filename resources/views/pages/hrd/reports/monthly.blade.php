@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Buat dan export rekap absensi bulanan.</p></div><a href="{{ route('api.hrd.reports.monthly-attendance.export', request()->query()) }}" class="btn btn-success">Export CSV</a></div><div class="card custom-card ka-card mb-3"><div class="card-body"><form method="POST" action="{{ route('hrd.reports.monthly-attendance.generate') }}" class="row g-2 ka-hrd-filter">@csrf<div class="col-6 col-md-2"><input type="number" name="year" value="{{ $year }}" class="form-control" placeholder="Tahun"></div><div class="col-6 col-md-2"><input type="number" name="month" value="{{ $month }}" min="1" max="12" class="form-control" placeholder="Bulan"></div><div class="col-6 col-md-2"><button class="btn btn-primary w-100">Generate</button></div><div class="col-6 col-md-2"><a href="{{ route('hrd.reports.monthly-attendance', ['year' => $year, 'month' => $month]) }}" class="btn btn-light w-100">Filter</a></div></form></div></div><div class="card custom-card ka-card"><div class="card-body"><div class="ka-hrd-card-list">@forelse($items as $item)<div class="ka-hrd-item"><div class="d-flex justify-content-between gap-2"><div><div class="ka-hrd-item-title">{{ $item->employee?->full_name ?? '-' }}</div><div class="ka-hrd-meta">{{ $item->employee?->department?->name ?? '-' }}</div></div><span class="ka-hrd-pill">{{ $item->total_present }} hadir</span></div><div class="ka-hrd-meta mt-2">Telat {{ $item->total_late }} · Sakit {{ $item->total_sick }} · Cuti {{ $item->total_leave }} · Izin {{ $item->total_permission }}</div><div class="ka-hrd-meta mt-1">Belum lengkap {{ $item->total_incomplete }} · Luar radius {{ $item->total_outside_radius }} · {{ $item->total_work_minutes }} menit kerja</div></div>@empty<div class="text-center text-muted py-4">Belum ada data. Generate dulu.</div>@endforelse</div><div class="table-responsive ka-hrd-table"><table class="table ka-table"><thead><tr><th>Karyawan</th><th>Departemen</th><th>Hadir</th><th>Telat</th><th>Sakit</th><th>Cuti</th><th>Izin</th><th>Belum Lengkap</th><th>Luar Radius</th><th>Menit Kerja</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->employee?->full_name ?? '-' }}</td><td>{{ $item->employee?->department?->name ?? '-' }}</td><td>{{ $item->total_present }}</td><td>{{ $item->total_late }}</td><td>{{ $item->total_sick }}</td><td>{{ $item->total_leave }}</td><td>{{ $item->total_permission }}</td><td>{{ $item->total_incomplete }}</td><td>{{ $item->total_outside_radius }}</td><td>{{ $item->total_work_minutes }}</td></tr>@empty<tr><td colspan="10" class="text-center text-muted py-4">Belum ada data. Generate dulu.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div></div>
@endsection
