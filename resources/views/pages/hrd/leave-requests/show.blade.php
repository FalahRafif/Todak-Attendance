@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Cek detail dan beri keputusan untuk pengajuan.</p></div><a href="{{ route('hrd.leave-requests') }}" class="btn btn-light">Kembali</a></div><div class="row g-3"><div class="col-lg-8"><div class="card custom-card ka-card"><div class="card-body"><h5>{{ $item->employee?->full_name ?? '-' }}</h5><p class="text-muted">{{ $item->employee?->department?->name ?? '-' }}</p><div class="row g-3"><div class="col-6 col-md-4"><div class="ka-detail-box"><b>Jenis</b><div>{{ friendly_label($item->leaveType?->description) }}</div></div></div><div class="col-6 col-md-4"><div class="ka-detail-box"><b>Periode</b><div>{{ $item->start_date?->format('d M Y') }} - {{ $item->end_date?->format('d M Y') }}</div></div></div><div class="col-6 col-md-4"><div class="ka-detail-box"><b>Total Hari</b><div>{{ $item->total_days }}</div></div></div><div class="col-12"><div class="ka-detail-box"><b>Alasan</b><div>{{ $item->reason ?? '-' }}</div></div></div><div class="col-12"><div class="ka-detail-box"><b>Alasan Ditolak</b><div>{{ $item->rejected_reason ?? '-' }}</div></div></div></div></div></div></div><div class="col-lg-4"><div class="card custom-card ka-card"><div class="card-body"><h5>Keputusan</h5><p>Status: <b>{{ friendly_label($item->status?->description) }}</b></p><form method="POST" action="{{ route('hrd.leave-requests.approve', $item->id) }}" class="mb-2">@csrf<textarea name="approval_note" class="form-control mb-2" placeholder="Catatan persetujuan"></textarea><button class="btn btn-success w-100">Setujui</button></form><form method="POST" action="{{ route('hrd.leave-requests.reject', $item->id) }}">@csrf<textarea name="rejected_reason" class="form-control mb-2" placeholder="Alasan ditolak" required></textarea><button class="btn btn-danger w-100">Tolak</button></form></div></div></div></div></div>
@endsection
