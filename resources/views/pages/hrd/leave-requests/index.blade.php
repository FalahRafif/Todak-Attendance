@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
@include('pages.admin.modules.partials.flash')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Tinjau pengajuan sakit, cuti, dan izin.</p></div></div><div class="card custom-card ka-card"><div class="card-body"><div class="ka-hrd-card-list">@forelse($items as $item)<a href="{{ route('hrd.leave-requests.show', $item->id) }}" class="ka-hrd-item"><div class="d-flex justify-content-between gap-2"><div><div class="ka-hrd-item-title">{{ $item->employee?->full_name ?? '-' }}</div><div class="ka-hrd-meta">{{ friendly_label($item->leaveType?->description) }} · {{ $item->total_days }} hari</div></div><span class="ka-hrd-pill">{{ friendly_label($item->status?->description) }}</span></div><div class="ka-hrd-meta mt-2">{{ $item->start_date?->format('d M Y') }} - {{ $item->end_date?->format('d M Y') }}</div></a>@empty<div class="text-center text-muted py-4">Belum ada pengajuan.</div>@endforelse</div><div class="table-responsive ka-hrd-table"><table class="table ka-table"><thead><tr><th>Karyawan</th><th>Jenis</th><th>Periode</th><th>Hari</th><th>Status</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->employee?->full_name ?? '-' }}</td><td>{{ friendly_label($item->leaveType?->description) }}</td><td>{{ $item->start_date?->format('Y-m-d') }} - {{ $item->end_date?->format('Y-m-d') }}</td><td>{{ $item->total_days }}</td><td>{{ friendly_label($item->status?->description) }}</td><td><a href="{{ route('hrd.leave-requests.show', $item->id) }}" class="btn btn-sm btn-primary">Detail</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Belum ada pengajuan.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div></div>
@endsection
