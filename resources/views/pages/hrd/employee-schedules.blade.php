@extends('layouts.admin.admin')
@section('title', $title)
@include('pages.hrd.partials.mobile-styles')
@section('content')
<div class="ka-hrd-shell"><div class="ka-hrd-toolbar"><div><h2 class="ka-page-title">{{ $title }}</h2><p class="ka-page-subtitle">Pantau jadwal kerja karyawan.</p></div></div><div class="card custom-card ka-card"><div class="card-body"><div class="ka-hrd-card-list">@forelse($items as $item)<div class="ka-hrd-item"><div class="d-flex justify-content-between gap-2"><div><div class="ka-hrd-item-title">{{ $item->employee?->full_name ?? '-' }}</div><div class="ka-hrd-meta">{{ $item->employee?->department?->name ?? '-' }}</div></div><span class="ka-hrd-pill">{{ $item->shift?->name ?? '-' }}</span></div><div class="ka-hrd-meta mt-2">{{ $item->schedule_date?->format('d M Y') ?? '-' }} · {{ $item->note ?? '-' }}</div></div>@empty<div class="text-center text-muted py-4">Belum ada jadwal.</div>@endforelse</div><div class="table-responsive ka-hrd-table"><table class="table ka-table"><thead><tr><th>Tanggal</th><th>Karyawan</th><th>Departemen</th><th>Shift</th><th>Catatan</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->schedule_date?->format('Y-m-d') ?? '-' }}</td><td>{{ $item->employee?->full_name ?? '-' }}</td><td>{{ $item->employee?->department?->name ?? '-' }}</td><td>{{ $item->shift?->name ?? '-' }}</td><td>{{ $item->note ?? '-' }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">Belum ada jadwal.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer bg-transparent">{{ $items->links() }}</div></div></div>
@endsection
